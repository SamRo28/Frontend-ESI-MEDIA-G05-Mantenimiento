import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.esimediag05Mantenimiento.app',
  appName: 'esimedia-mantenimiento',
  webDir: 'dist/EsiMediaFE/browser'
};

export default config;
